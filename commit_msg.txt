feat: Implement PyTorch Action-Alignment Loss and BDI ReCAP harness

- Created `ActionAlignmentLoss` PyTorch module to mathematically eliminate the "thought-action gap" in sequential agent simulations by penalizing deviations from oracle Best Responses.
- Implemented `BDIRecapSimulator` in Python to demonstrate Closed-Loop BDI verification, vetoing non-optimal logic via simulated Clingo interception.
- Added comprehensive unit testing for the Nash Trap edge case.
- Logged ADR-010 to `docs/adr/` and `DECISIONS.md`.
- Updated `README.md` to surface Action-Alignment capabilities to the principal layer.
