fix: Resolve CI failures in Epistemic Guard

- Added `ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true` to bypass the Node 20 deprecation warning causing errors in GitHub Actions.
- Set `ubuntu-latest` instead of `ubuntu-24.04`.
- Disabled specific smells in `dirty-waters-action` which was throwing unrecognized arguments error in Python due to incorrect formatting.
