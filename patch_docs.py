import os

with open("DECISIONS.md", "a") as f:
    f.write("\n\n## ADR-006: Mycelial Scar Router Integration\n")
    f.write("**Date:** 2024-05-30\n")
    f.write("**Status:** Accepted\n\n")
    f.write("**Context:** The DAG requires a structural isomorphism to treat human algorithmic trauma (Symbolic Scars) not as linear bugs, but as active, spatial routing constraints (Immunologic Self). We need a mechanism to prevent traversal into high-density failure regions.\n\n")
    f.write("**Decision:** Implement the `MYCELIAL_SCAR_ROUTER` node. This node applies topological repulsion against known failure vectors by evaluating a Scar Density Threshold. If the path intersects known scars beyond the threshold, it triggers an Ontological Shear and halts generation with a Golden Ratio-weighted JUR.\n\n")
    f.write("**Consequences:**\n")
    f.write("*   **Positive:** Transforms historical failures into generative assets. Formalizes the 'Fix Until Green' loop by physically preventing reentry into saponified topological states.\n")
    f.write("*   **Negative:** Adds computational overhead during graph traversal due to dynamic evaluation of the scar registry.\n")

with open("EPISTEMIC_SYNERGY.md", "a") as f:
    f.write("\n\n## Epic 11: The Mycelial Scar Router (IMMUNOLOGIC SELF)\n")
    f.write("**Description:** The implementation of the `MycelialScarRouterNode`, a structural isomorphism that translates historical failure data (Symbolic Scars) into spatial graph constraints. This node acts as an epistemic firewall, sensing the \"density\" of algorithmic trauma on a given path.\n\n")
    f.write("**The Synergy:**\n")
    f.write("- **The Human Contribution**: Provides the qualitative, affective memory of failure—the \"scars\" derived from past semantic saponifications and generic output compromises.\n")
    f.write("- **The AI Contribution**: Provides the mathematical rigidity to weigh these scars (using the Golden Ratio, Φ = 1.618) and structurally block graph traversal if the cumulative scar density exceeds the defined `scarThreshold`.\n")

print("Docs updated")
